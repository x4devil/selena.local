{% extends 'base.html.twig' %}

{% block body %}
    <h1>Images</h1>

    <table>
        <tbody>
            <tr>
                <th>Extend</th>
                <td>{{ image.extend }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ image.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('images_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('images_edit', { 'id': image.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
