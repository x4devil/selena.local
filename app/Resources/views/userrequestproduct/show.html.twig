{% extends 'base.html.twig' %}

{% block body %}
    <h1>UserRequestProduct</h1>

    <table>
        <tbody>
            <tr>
                <th>Price</th>
                <td>{{ userRequestProduct.price }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ userRequestProduct.amount }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ userRequestProduct.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('userrequestproduct_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('userrequestproduct_edit', { 'id': userRequestProduct.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
